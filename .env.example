# Local development configuration

The app requires no environment variables to run. Keyword data comes from
Apple's public iTunes Search API directly in the browser.

```bash
npm install
npm run dev
```

No `.env.local` is needed for development or for the Cloudflare deploy — the
web Worker has no secrets or bindings.
